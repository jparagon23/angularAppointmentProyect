import { NameShortFormatPipe } from './name-short-format.pipe';

describe('NameShortFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new NameShortFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
